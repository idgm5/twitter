<%= form_with(model: tweet, local: true) do |form| %>
  <% if tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
      <% tweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p style="display: flex; flex-direction: column">
    <p>Chars left: <span id="counter" data-maximum-length="<%= @maximum_length %>"><%= @maximum_length %></span></p>
    <%= form.text_area :body, maxlength: @maximum_length, placeholder: "What is happening?", :id => 'body', :class => 'tweet-body'%>
  </p>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
